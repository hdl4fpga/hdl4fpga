[ ] device-specific report decoder as separate module
[x] get report with addressed write instead of shifting
[ ] test will CRC reject bad data
[x] retry setup packets until ACK is received
[ ] send keepalive signals if interval >1 ms (>12 bit)
[ ] let SIE handle BUS_RESET
