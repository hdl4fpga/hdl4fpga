@ECHO OFF
FOR %%F IN (%0) DO SET DIRNAME=%%~DPF
"%DIRNAME%"SCOPE -P %1 -D %2 -H %3 > DUMP.DAT
SET I=1
:NEXT
	"%DIRNAME%"SCOPE -P %1 -D %2 -H %3 > DUMP1.DAT
	CMP DUMP0 DUMP
	IF %ERRORLEVEL% EQU 0 (
		ECHO DUMPED %I% OK
		SET /A I=%I%+1
		GOTO NEXT
	)
