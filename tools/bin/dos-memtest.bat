@ECHO OFF
FOR %%F IN (%0) DO SET DIRNAME=%%~DPF
"%DIRNAME%"SCOPE -p %1 -d %2 -h %3 > DUMP0
SET I=1
:NEXT
	"%DIRNAME%"SCOPE -p %1 -d %2 -h %3 > DUMP0
	CMP dump0 dump
	IF %ERRORLEVEL% EQU 0 (
		ECHO READ NUMBER %I%
		SET /A I=%I%+1
		GOTO NEXT
	)
